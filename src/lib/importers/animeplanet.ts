/**
 * Removed: the Anime-Planet profile-list importer (no profile to import).
 * Anime-Planet is now used as a per-series info source — see the
 * /api/proxy/animeplanet route and src/components/series/ApInfoCard.tsx.
 */
export {};
